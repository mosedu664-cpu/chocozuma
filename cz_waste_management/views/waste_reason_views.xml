<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ==================== Waste Reason Views ==================== -->

    <!-- List View -->
    <record id="waste_reason_view_list" model="ir.ui.view">
        <field name="name">waste.reason.list</field>
        <field name="model">waste.reason</field>
        <field name="arch" type="xml">
            <list string="Waste Reasons" editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="description"/>
                <field name="active" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="waste_reason_view_form" model="ir.ui.view">
        <field name="name">waste.reason.form</field>
        <field name="model">waste.reason</field>
        <field name="arch" type="xml">
            <form string="Waste Reason">
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="sequence"/>
                            <field name="active"/>
                        </group>
                        <group>
                            <field name="description"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_waste_reason" model="ir.actions.act_window">
        <field name="name">Waste Reasons</field>
        <field name="res_model">waste.reason</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first waste reason
            </p>
            <p>
                Waste reasons categorize why items were wasted (e.g., Expired, Burnt, Dropped).
            </p>
        </field>
    </record>

</odoo>
