<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="cnc_pos_kot.KotReceipt">
        <div class="pos-receipt">

            <div class="kitchen-order-title">
                <h2>Kitchen Order Ticket</h2>
            </div>

            <div class="order-info">
                <div t-if="props.data.name" class="pos-receipt-order-data">
                    <span><t t-esc="props.data.name"/></span>
                </div>
                <div t-if="props.data.date.localestring" class="pos-receipt-order-data">
                    <span class="label">Date:</span>
                    <span><t t-esc="props.data.date.localestring"/></span>
                </div>

                <div t-if="props.data.client" class="pos-receipt-order-data">
                    <span class="label">Customer:</span>
                    <span><t t-esc="props.data.client.name"/></span>
                </div>
                <div t-if="props.data.table" class="pos-receipt-order-data">
                    <span class="label">Table:</span>
                    <span><t t-esc="props.data.table.name"/></span>
                </div>
                <div t-if="props.data.cashier" class="pos-receipt-order-data">
                    <span class="label">Server:</span>
                    <span><t t-esc="props.data.cashier"/></span>
                </div>
            </div>
            <div class="orderlines">
                <table class="orderlines-table">
                    <thead>
                        <tr>
                            <th class="qty-col">
                                Product</th>
                            <th class="product-col">Qty</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr t-foreach="props.data.orderlines" t-as="line" t-key="line.id">
                            <li class="orderline position-relative d-flex align-items-center p-2 lh-sm cursor-pointer"
                                t-attf-class="{{ line.comboParent ? 'orderline-combo ms-4 fst-italic' : '' }}"
                                t-att-class="props.class">

            <div class="d-flex flex-column w-100 gap-1">
                <div class="d-flex justify-content-between">
                    <div class="product-name d-inline-block flex-grow-1 fw-bolder pe-1 text-truncate">
                        <span class="text-wrap" t-esc="line.productName"/>
                        <t t-slot="product-name"/>
                    </div>
                    <li class="price-per-unit">
                        <span class="qty px-1 border rounded text-bg-view fw-bolder me-1" t-esc="line.qty"/>
                 </li>
                </div>
                <ul class="info-list d-flex flex-column">
                    <li t-if="line.customerNote"
                        class="customer-note w-100 p-2 mt-2 rounded text-break text-bg-warning bg-opacity-25">
                        <i class="fa fa-sticky-note me-1" role="img" aria-label="Customer Note" title="Customer Note"/>
                        <t t-esc="line.customerNote"/>
                    </li>
                    <div class="internal-note-container d-flex gap-2">
                        <t t-foreach="line.internalNote?.split?.('\n') or []" t-as="note" t-key="note_index">
                            <li t-if="note.trim() !== ''" t-esc="note"
                                class="internal-note badge mt-2 p-2 rounded-pill bg-info text-info bg-opacity-25"
                                style="font-size: 0.85rem;"/>
                        </t>
                    </div>
                </ul>
            </div>
        </li>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- General Note -->
            <div t-if="props.data.note" class="pos-receipt-center-align">
                <div class="general-note">
                    <div class="note-label">Order Note:</div>
                    <div class="note-content"><t t-esc="props.data.note"/></div>
                </div>
            </div>

            <div class="pos-receipt-order-data kot-footer">
                <div class="print-time">
                    <span class="label">Printed:</span>
                    <span><t t-esc="new Date().toLocaleTimeString()"/></span>
                </div>
            </div>
        </div>
    </t>
</templates>